import React from 'react'
import Carousel from '../../components/Carousel/Carousel'

function WorksDisplay({title}) {
  return (
    <div>
        WorksDisplay
        <Carousel title={title}/>
    </div>
  )
}

export default WorksDisplay